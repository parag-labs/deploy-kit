{
  "name": "deploy-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic, secure-by-default deployment plan core (TypeScript port).",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
